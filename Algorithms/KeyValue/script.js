const arr =  [
    { type:'A', value:'Tesla' },	
    { type:'A', value:'GMC' },
    { type:'B', value:'KIA'},
    { type:'C', value:'Dodge' },
    { type:'C', value:'Ford' }
    ]
    

    const keyValue=()=>{
        for(let i=0; i<arr.length; i++){
        const {type, value}=arr[i];
          // console.log(type, value)
        
        }
    }

    keyValue()